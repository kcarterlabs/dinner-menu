# Environment variables for production deployment with Traefik

# Cloudflare API Token for DNS challenge (recommended - more secure)
# Get from: https://dash.cloudflare.com/profile/api-tokens
# Create token with: Zone/DNS/Edit + Zone/Zone/Read permissions for kcarterlabs.tech
CF_DNS_API_TOKEN=your-cloudflare-api-token-here

# Let's Encrypt email for certificate notifications
ACME_EMAIL=your-email@example.com

# MongoDB root password (choose a strong password)
# Can also be set as an environment variable on the Docker host
MONGO_PASSWORD=your-secure-mongodb-password-here

# Weather API key (optional - for weather features)
RAPID_API_FORECAST_KEY=your-rapid-api-key
